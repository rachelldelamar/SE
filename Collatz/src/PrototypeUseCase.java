package collatz.src;
/*
class PrototypeUseCase {
 public void CollatzSequence(startingNumberForSequence userInput) {
  //network boundary connects User to CE1
    //User input is one integer positive less than 1 million
    if(userInput.vaildValue()){
        //Send integer to CE1 for computations
        CollatzSequenceMachine(userInput);
    }
  //Conceptual Boundary connects CEs together 
    //CE1 takes integer, sends to CE2 if Integer is even
    //Or CE1 sends integer to CE3 if odd
    //CE1 ends computation if an integer value is 1
    if(userInput.even()){
        userInput.CE2();
    }else if(userInput.odd()){
        userInput.CE3();
    }else if(userInput.equalsOne()){
        userInput.endComputation();
    }
  //process boundary connect CE with DS
    //Takes output from CE1 and processes the output in user-specific format. 
    userInput.Display();
  }  
 }
}
*/
